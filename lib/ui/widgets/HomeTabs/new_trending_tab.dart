import 'package:flutter/material.dart';

class NewTrendingTab extends StatelessWidget {
  const NewTrendingTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('New & Trending Tab'),
    );
  }
}
