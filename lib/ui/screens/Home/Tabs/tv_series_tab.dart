import 'package:flutter/material.dart';

class TvSeriesTab extends StatelessWidget {
  const TvSeriesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('TV Series Tab'),
    );
  }
}
